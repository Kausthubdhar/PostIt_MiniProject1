<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white h-screen flex flex-col items-center">
    <!-- Navbar -->
    <header class="w-full bg-zinc-800 p-4 shadow-md">
        <div class="container mx-auto flex justify-between items-center">
            <a href="/home"><h1 class="text-3xl font-bold text-blue-400">PostIt</h1></a>
            <nav>
                <a href="/home" class="text-zinc-400 hover:text-white px-3">Home</a>
                <a href="#" class="text-zinc-400 hover:text-white px-3">Profile</a>
                <a href="/logout" class="px-4 py-2 bg-red-500 text-white font-bold rounded-lg hover:bg-red-600 transition duration-300 ease-in-out shadow-lg">Logout</a>
            </nav>
        </div>
    </header>

    <!-- Main Content -->
    <main class="w-full flex-1 flex flex-col items-center justify-center p-4">
        <!-- Greeting -->
        <div class="text-center mb-8">
            <h2 class="text-4xl font-bold mb-4 text-blue-400">Edit</h2>
            <p class="text-zinc-300">Edit your post here!</p>
        </div>

        <!-- Post Form -->
        <form action="/edit/<%= post._id %>" method="POST" class="w-full max-w-lg bg-zinc-800 p-6 rounded-lg shadow-lg">
            <div class="mb-4">
                <label for="post" class="block text-zinc-300 font-bold mb-2">Edit your post</label>
                <textarea name="content" rows="4" class="resize-none w-full px-3 py-2 text-white bg-zinc-800 border border-zinc-300 rounded focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent" ><%= post.content %></textarea>
            </div>
            <div class="flex justify-between items-center">
                <button type="submit" class="bg-green-500 hover:bg-green-600 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Save</button>
            </div>
        </form>
    </main>
</body>
</html>